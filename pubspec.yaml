name: hyrox_challenges
description: Application Hyrox Challenges Flutter + Firebase
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  cloud_firestore: ^4.11.0
  firebase_auth: ^4.10.0
  provider: ^6.0.5
  uuid: ^4.5.1
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/avatars/
